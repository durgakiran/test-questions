/**
 * given node of a singly linked list and a number(N), find the node N nodes before the given node. 
 */

class Node {
  constructor(value) {
    this.value = value;
    this.next = null;
  }
}



class SinglyLinkedList {
  constructor() {
    this.head =  null;
    this.tail = null;
    this.length = 0;
  }

  //--- linked list methods start here
  //--- linked list methods end here
}

function findTheNode () {
  /**
   * Since there is no previous node reference it is not 
   * possible to find the node.
   */

  // impossible
}





